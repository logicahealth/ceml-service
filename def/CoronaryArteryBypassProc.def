CoronaryArteryBypassProc model is a procedure model. In a procedure model the key code is Procedure_KEY_ECID and data describes the procedure being performed.
In the CoronaryArteryBypassProc model data is CoronaryArteryBypass_ECID which captures information surrounding a coronary artery bypass which means the performance of a surgical procedure to treat ischemic heart disease.

