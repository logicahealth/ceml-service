PupilsConsensualToLightAssert is an assertion model. In an assertion model, the key is set to "Assertion_KEY_ECID", and data is the condition being asserted. 
For example, the AbdominalTendernessAssert model has a key of "Assertion_KEY_ECID" and a data of "AbdominalTenderness_ECID".
In the PupilsConsensualToLightAssert model, data is PupilsConsensualToLight_ECID which means an indication of whether one eye reacts when the opposite eye is exposed to light.

