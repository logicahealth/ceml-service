EyeForeignBodyRemovalProc model is a procedure model. In a procedure model the key code is Procedure_KEY_ECID and data describes the procedure being performed.
In the EyeForeignBodyRemovalProc model the data is "EyeForeignBodyRemoval_KEY_ECID" which  captures the key information about the medical procedure or act of removing an object not native to the eye from the eye.

